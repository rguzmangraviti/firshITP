import React from 'react';
import {Text} from 'react-native';
import styles from '../styles/styles';

const Title = () => {
  return <Text style={styles.title}>Título</Text>;
};

export default Title;
